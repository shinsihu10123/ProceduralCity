import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { EconomicWorld } from '../src/core/world-v10.js';
import { COUNTRY_SEEDS } from '../src/config/countries.js';

const scales=(process.env.DIAG_SCALES||'compact,baseline').split(',').map(x=>x.trim()).filter(Boolean);
const seeds=(process.env.DIAG_SEEDS||'ECON-RV02-A,ECON-RV02-B,ECON-RV02-C').split(',').map(x=>x.trim()).filter(Boolean);
const months=Math.max(1,Number(process.env.DIAG_MONTHS||12));
const outputJson=process.env.OUTPUT_JSON?resolve(process.env.OUTPUT_JSON):null;
const EPS=1e-8,TOL=1e-7;
const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
const sum=a=>a.reduce((s,v)=>s+finite(v),0),mean=a=>a.length?sum(a)/a.length:0,ratio=(a,b)=>Math.abs(finite(b))>EPS?finite(a)/finite(b):0,clone=v=>structuredClone(v);
function transformedSeeds(){return COUNTRY_SEEDS.map(seed=>({...seed,initialPrice:Math.max(EPS,finite(seed.initialWage,finite(seed.initialPrice,1)))}));}
function createWorld(scaleProfile,seedText){const original=COUNTRY_SEEDS.map(clone);COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...transformedSeeds());try{return new EconomicWorld(seedText,{scaleProfile,healthCheckInterval:0});}finally{COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...original);}}
function gdpResidual(m){return finite(m?.gdp)-(finite(m?.consumption)+finite(m?.grossInvestment)+finite(m?.publicInvestment)+finite(m?.governmentConsumption)+finite(m?.inventoryInvestment)+finite(m?.netExports));}
function fingerprint(w){return {month:w.month,rng:clone(w.rng),countries:clone(w.countries),ledgerEntries:clone(w.ledger.entries),accounting:w.countries.map(c=>({id:c.id,report:w.accountingReport(c.id)}))};}
function installVariant(world,variant){world.__rv07P32={skippedFirmDebtDue:0,capexCallsSkipped:0};if(variant.includes('firm-debt-holiday')){const service=world.banking.serviceDebt.bind(world.banking);world.banking.serviceDebt=(country,month)=>{const firmIds=new Set(country.firms.map(f=>f.id));const saved=[];for(const loan of country.loans){if(loan.status==='active'&&firmIds.has(loan.borrowerId)&&month>=loan.nextPaymentMonth){saved.push({loan,nextPaymentMonth:loan.nextPaymentMonth});loan.nextPaymentMonth=month+1;world.__rv07P32.skippedFirmDebtDue+=1;}}const out=service(country,month);for(const x of saved)x.loan.nextPaymentMonth=x.nextPaymentMonth;return out;};}if(variant.includes('no-capex')){world.supply.clearInvestmentMarket=(country,month,metrics)=>{world.__rv07P32.capexCallsSkipped+=1;return metrics;};}}
function runVariant(variant,scaleProfile,seed,horizon){const world=createWorld(scaleProfile,seed);installVariant(world,variant);const rows=[];for(let i=0;i<horizon;i++){world.stepMonth();for(const c of world.countries)rows.push({variant,scaleProfile,seed,month:world.month,countryId:c.id,unemployment:finite(c.macro?.unemployment),exits:finite(c.macro?.firmExits),wageArrears:finite(c.macro?.wageArrears),goodsFulfillment:1-finite(c.macro?.unmetDemandRatio),inputShortage:finite(c.macro?.inputShortageUnits),resourceOutput:finite(c.macro?.resourceOutput),materialsOutput:finite(c.macro?.materialsOutput),consumerOutput:finite(c.macro?.consumerGoodsOutput),grossInvestment:finite(c.macro?.grossInvestment),firmCash:finite(c.macro?.firmCash),newCredit:finite(c.macro?.newCredit),principalRepaid:finite(c.macro?.principalRepaid),interestPaid:finite(c.macro?.interestPaid),loanDefaults:finite(c.macro?.loanDefaults),gdp:finite(c.macro?.gdp),gdpResidual:gdpResidual(c.macro),ledgerOk:world.ledger.verifyCountry(c.id)?.ok===true});}const health=world.forceHealthCheck();assert.ok(health.ok,`${variant}/${scaleProfile}/${seed}: health failed`);return {variant,scaleProfile,seed,world,rows,skippedFirmDebtDue:world.__rv07P32.skippedFirmDebtDue,capexCallsSkipped:world.__rv07P32.capexCallsSkipped,health,fingerprint:fingerprint(world)};}
const variants=['unit-basis-control','unit-basis-firm-debt-holiday','unit-basis-no-capex','unit-basis-firm-debt-holiday-no-capex'];
const determinism=[];for(const v of variants)for(const s of scales){const seed=`ECON-RV07-P32-DET-${v}-${s}`,h=Math.min(3,months);const a=runVariant(v,s,seed,h).fingerprint,b=runVariant(v,s,seed,h).fingerprint,exact=JSON.stringify(a)===JSON.stringify(b);assert.ok(exact,`${v}/${s}: nondeterministic`);determinism.push({variant:v,scaleProfile:s,exact});}
const runs=[];for(const v of variants)for(const s of scales)for(const seed of seeds)runs.push(runVariant(v,s,seed,months));const rows=runs.flatMap(r=>r.rows);
const windows=[{id:'M1-3',from:1,to:Math.min(3,months)},{id:'M4-6',from:4,to:Math.min(6,months)},{id:'M7-9',from:7,to:Math.min(9,months)},{id:'M10-12',from:10,to:months},{id:'FULL',from:1,to:months}].filter(w=>w.from<=w.to);
function aggregate(rs){return {countryMonths:rs.length,meanUnemployment:mean(rs.map(r=>r.unemployment)),totalExits:sum(rs.map(r=>r.exits)),meanWageArrears:mean(rs.map(r=>r.wageArrears)),meanGoodsFulfillment:mean(rs.map(r=>r.goodsFulfillment)),meanInputShortage:mean(rs.map(r=>r.inputShortage)),meanResourceOutput:mean(rs.map(r=>r.resourceOutput)),meanMaterialsOutput:mean(rs.map(r=>r.materialsOutput)),meanConsumerOutput:mean(rs.map(r=>r.consumerOutput)),meanGrossInvestment:mean(rs.map(r=>r.grossInvestment)),meanFirmCash:mean(rs.map(r=>r.firmCash)),meanNewCredit:mean(rs.map(r=>r.newCredit)),meanPrincipalRepaid:mean(rs.map(r=>r.principalRepaid)),meanInterestPaid:mean(rs.map(r=>r.interestPaid)),totalDefaults:sum(rs.map(r=>r.loanDefaults)),meanGdp:mean(rs.map(r=>r.gdp))};}
const summary=[];for(const v of variants)for(const s of scales)for(const w of windows)summary.push({variant:v,scaleProfile:s,window:w.id,...aggregate(rows.filter(r=>r.variant===v&&r.scaleProfile===s&&r.month>=w.from&&r.month<=w.to))});
const comparison={};for(const s of scales){comparison[s]={};const controlByWindow=Object.fromEntries(windows.map(w=>[w.id,summary.find(x=>x.variant==='unit-basis-control'&&x.scaleProfile===s&&x.window===w.id)]));for(const v of variants.filter(x=>x!=='unit-basis-control')){comparison[s][v]={};for(const w of windows){const c=controlByWindow[w.id],a=summary.find(x=>x.variant===v&&x.scaleProfile===s&&x.window===w.id);comparison[s][v][w.id]={unemploymentDifference:a.meanUnemployment-c.meanUnemployment,exitDifference:a.totalExits-c.totalExits,wageArrearsDifference:a.meanWageArrears-c.meanWageArrears,goodsFulfillmentDifference:a.meanGoodsFulfillment-c.meanGoodsFulfillment,inputShortageDifference:a.meanInputShortage-c.meanInputShortage,consumerOutputRatio:ratio(a.meanConsumerOutput,c.meanConsumerOutput),materialsOutputRatio:ratio(a.meanMaterialsOutput,c.meanMaterialsOutput),firmCashDifference:a.meanFirmCash-c.meanFirmCash,gdpDifference:a.meanGdp-c.meanGdp};}}}
const debtRuns=runs.filter(r=>r.variant.includes('firm-debt-holiday')),capexRows=rows.filter(r=>r.variant.includes('no-capex')),maxGdpResidual=Math.max(0,...rows.map(r=>Math.abs(r.gdpResidual)));
const gates={deterministicReplayExact:determinism.every(x=>x.exact),allHealthy:runs.every(r=>r.health?.ok===true),completeCoverage:rows.length===variants.length*scales.length*seeds.length*months*4,debtHolidayActuallyEncountered:debtRuns.reduce((s,r)=>s+r.skippedFirmDebtDue,0)>0,noCapexVariantsHaveZeroPrivateInvestment:capexRows.every(r=>Math.abs(r.grossInvestment)<TOL),ledgerCountriesOk:rows.every(r=>r.ledgerOk),gdpIdentityReconciled:maxGdpResidual<TOL,finiteRows:rows.every(r=>Number.isFinite(r.unemployment)&&Number.isFinite(r.firmCash))};gates.ok=Object.values(gates).every(Boolean);assert.ok(gates.ok,`WP-RV07-P32 gates failed: ${JSON.stringify(gates)}`);
console.table(summary.filter(x=>x.window==='FULL').map(x=>({variant:x.variant,scale:x.scaleProfile,u:+x.meanUnemployment.toFixed(4),exits:x.totalExits,arrears:+x.meanWageArrears.toFixed(1),fulfill:+x.meanGoodsFulfillment.toFixed(4),consumer:+x.meanConsumerOutput.toFixed(2),capex:+x.meanGrossInvestment.toFixed(1),cash:+x.meanFirmCash.toFixed(1),principal:+x.meanPrincipalRepaid.toFixed(1),interest:+x.meanInterestPaid.toFixed(1)})));
console.log('WP_RV07_P32_COMPARISON',JSON.stringify(comparison));
console.log('WP_RV07_P32_GATES',JSON.stringify(gates));
const payload={workPackage:'WP-RV07-P32',title:'Non-operating firm cash-drain causal matrix',generatedAt:new Date().toISOString(),configuration:{variants,scales,seeds,months},determinism,gates,reconciliation:{maxGdpResidual},summary,comparison,runInterventions:runs.map(r=>({variant:r.variant,scaleProfile:r.scaleProfile,seed:r.seed,skippedFirmDebtDue:r.skippedFirmDebtDue,capexCallsSkipped:r.capexCallsSkipped})),rows};if(outputJson){mkdirSync(dirname(outputJson),{recursive:true});writeFileSync(outputJson,JSON.stringify(payload,null,2));console.log('WP_RV07_P32_OUTPUT',outputJson);}
