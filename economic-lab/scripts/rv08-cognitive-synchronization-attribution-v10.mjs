import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { EconomicWorld } from '../src/core/world-v10.js';
import { COUNTRY_SEEDS } from '../src/config/countries.js';

const seed=(process.env.DIAG_SEED||'ECON-RV02-A').trim();
const months=Math.max(24,Number(process.env.DIAG_MONTHS||36));
const variants=(process.env.DIAG_VARIANTS||'control,legacy-firms,legacy-households,legacy-both').split(',').map(x=>x.trim()).filter(Boolean);
const outputJson=process.env.OUTPUT_JSON?resolve(process.env.OUTPUT_JSON):null;
const EPS=1e-8,TOL=1e-7;
const F=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
const C=v=>structuredClone(v);
const S=a=>a.reduce((s,v)=>s+F(v),0);
const M=a=>a.length?S(a)/a.length:0;
const CL=(x,l,h)=>Math.max(l,Math.min(h,F(x)));

function transformedSeeds(){return COUNTRY_SEEDS.map(s=>({...s,initialPrice:Math.max(EPS,F(s.initialWage,F(s.initialPrice,1)))}));}
function makeWorld(){const old=COUNTRY_SEEDS.map(C);COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...transformedSeeds());try{return new EconomicWorld(seed,{scaleProfile:'baseline',healthCheckInterval:0});}finally{COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...old);}}
function supplierMean(c,p){const a=c.firms.filter(f=>f.active!==false&&f.product===p&&F(f.price)>EPS);return a.length?M(a.map(f=>f.price)):0;}
function unconstrainedPlan(f){const anchor=Math.max(2,F(f.previousSales),F(f.targetInventory)*.42),expected=anchor*(1+CL(f.beliefs?.demandGrowth||0,-.18,.22)),replen=Math.max(0,F(f.targetInventory)-F(f.inventory));return Math.max(0,expected*.72+replen);}
function installNormalization(w){const target=new Set(['MATERIALS','CONSUMER']),done=new Set();w.__bcNorm=0;const original=w.supply.planProduction.bind(w.supply);w.supply.planProduction=c=>{const out=original(c);if(done.has(c.id))return out;const prices={raw_material:supplierMean(c,'raw_material'),processed_material:supplierMean(c,'processed_material')};for(const f of c.firms.filter(x=>x.active!==false&&target.has(x.industryId))){const inputCost=F(f.inputPerOutput)*(f.inputProduct?F(prices[f.inputProduct]):0),margin=F(f.price)-inputCost,payroll=F(f.wage)*F(f.workers),cap=F(f.capacity),required=margin>EPS&&cap>EPS?payroll/(margin*cap):Infinity,factor=Number.isFinite(required)?Math.max(1,required):1;if(factor>1+TOL){f.productivity*=factor;f.capacity=cap*factor;f.desiredProduction=Math.max(0,Math.min(f.capacity*1.08,unconstrainedPlan(f)));w.__bcNorm++;}}done.add(c.id);return out;};}
function gdpResidual(m){return F(m?.gdp)-(F(m?.consumption)+F(m?.grossInvestment)+F(m?.publicInvestment)+F(m?.governmentConsumption)+F(m?.inventoryInvestment)+F(m?.netExports));}
function actionStats(values){const a=values.filter(Boolean),counts=new Map();for(const v of a)counts.set(v,(counts.get(v)||0)+1);if(!a.length)return{n:0,topShare:0,entropy:0,unique:0};const cs=[...counts.values()],top=Math.max(...cs)/a.length,entropy=-S(cs.map(k=>{const p=k/a.length;return p*Math.log(p);})),den=Math.log(Math.max(2,counts.size));return{n:a.length,topShare:top,entropy:den>0?entropy/den:0,unique:counts.size};}
function latestDecision(a){const d=a?.cognition?.decisions||[];return d.length?d[d.length-1]?.selected||null:null;}
function firmAction(f){return f.currentPlan?.selected||latestDecision(f);}
function householdAction(h){return h.lastTrace?.selected||latestDecision(h);}
function applyVariant(w,variant){const legacyF=variant==='legacy-firms'||variant==='legacy-both',legacyH=variant==='legacy-households'||variant==='legacy-both';for(const c of w.countries){for(const f of c.firms)if(legacyF&&f.cognition)f.cognition.enabled=false;for(const h of c.households)if(legacyH&&h.cognition)h.cognition.enabled=false;}if(legacyF){const original=w.createEntrant.bind(w);w.createEntrant=(country,industryId)=>{const f=original(country,industryId);if(f.cognition)f.cognition.enabled=false;return f;};}return{legacyF,legacyH};}
function fingerprint(w){return w.countries.map(c=>[c.id,F(c.macro?.unemployment),F(c.macro?.gdp),F(c.macro?.realOutput),F(c.macro?.wageArrears),F(c.macro?.activeFirms),F(c.macro?.newCredit),F(c.macro?.governmentTransfers)]);}
function assertSame(a,b,label){assert.equal(a.length,b.length,label);for(let i=0;i<a.length;i++){assert.equal(a[i].length,b[i].length,label);for(let j=0;j<a[i].length;j++){if(typeof a[i][j]==='string')assert.equal(a[i][j],b[i][j],label);else assert.ok(Math.abs(F(a[i][j])-F(b[i][j]))<1e-9,`${label} ${i}/${j}`);}}}
function noninterferenceGate(){const a=makeWorld(),b=makeWorld();for(const w of [a,b]){for(const c of w.countries)Object.defineProperty(c,'__diagnosticExactLaborRuntime',{value:true,writable:true,configurable:true,enumerable:false});installNormalization(w);}for(let i=0;i<6;i++){a.stepMonth();b.stepMonth();assertSame(fingerprint(a),fingerprint(b),`control replay month ${i+1}`);}return true;}

function runOne(variant){const w=makeWorld();for(const c of w.countries)Object.defineProperty(c,'__diagnosticExactLaborRuntime',{value:true,writable:true,configurable:true,enumerable:false});installNormalization(w);const mode=applyVariant(w,variant),rows=[];for(let i=0;i<months;i++){w.stepMonth();for(const c of w.countries){const active=c.firms.filter(f=>f.active!==false),consumer=active.filter(f=>f.industryId==='CONSUMER'),fs=actionStats(active.map(firmAction)),cs=actionStats(consumer.map(firmAction)),hs=actionStats(c.households.map(householdAction));rows.push({month:w.month,countryId:c.id,firmTop:fs.topShare,firmEntropy:fs.entropy,consumerTop:cs.topShare,consumerEntropy:cs.entropy,houseTop:hs.topShare,houseEntropy:hs.entropy,u:F(c.macro?.unemployment),gdp:F(c.macro?.gdp),output:F(c.macro?.realOutput),arrears:F(c.macro?.wageArrears),active:F(c.macro?.activeFirms),exits:F(c.macro?.firmExits),entries:F(c.macro?.firmEntries),credit:F(c.macro?.newCredit),transfers:F(c.macro?.governmentTransfers),consumption:F(c.macro?.consumption)});}}
 const health=w.forceHealthCheck(),accountingOk=w.countries.every(c=>w.accounting.verifyCountry(c,w.ledger,w.month)?.ok!==false),ledgerOk=w.countries.every(c=>w.ledger.verifyCountry(c.id)?.ok===true),gdpOk=w.countries.every(c=>Math.abs(gdpResidual(c.macro))<1e-5);assert.ok(accountingOk&&ledgerOk&&gdpOk,`${seed}/${variant}: accounting`);const terminal=rows.filter(r=>r.month===months),late=rows.filter(r=>r.month>=13);return{seed,variant,months,...mode,healthOk:health.ok,accountingOk,ledgerOk,gdpOk,normalizationApps:w.__bcNorm,meanFirmTop:M(rows.map(r=>r.firmTop)),meanFirmEntropy:M(rows.map(r=>r.firmEntropy)),meanConsumerTop:M(rows.map(r=>r.consumerTop)),meanConsumerEntropy:M(rows.map(r=>r.consumerEntropy)),meanHouseTop:M(rows.map(r=>r.houseTop)),meanHouseEntropy:M(rows.map(r=>r.houseEntropy)),meanUnemployment:M(rows.map(r=>r.u)),lateUnemployment:M(late.map(r=>r.u)),terminalUnemployment:M(terminal.map(r=>r.u)),terminalGdp:M(terminal.map(r=>r.gdp)),terminalOutput:M(terminal.map(r=>r.output)),terminalArrears:M(terminal.map(r=>r.arrears)),terminalActive:F(M(terminal.map(r=>r.active))),totalExits:S(rows.map(r=>r.exits)),totalEntries:S(rows.map(r=>r.entries)),totalCredit:S(rows.map(r=>r.credit)),totalTransfers:S(rows.map(r=>r.transfers)),lateConsumption:M(late.map(r=>r.consumption))};}

const noninterference=noninterferenceGate();const runs=variants.map(runOne);const report={workPackage:'WP-RV08-R4-BC',title:'Cognitive Synchronization Attribution Ablation',note:'Diagnostic factorial using existing cognitive vs existing legacy decision paths. No parameter tuning, cash transfer, accounting reorder, bankruptcy change, or canonical repair.',generatedAt:new Date().toISOString(),configuration:{seed,months,variants,base:'materials-consumer'},gates:{completeCoverage:runs.length===variants.length,controlReplayBitExact:noninterference,accountingOk:runs.every(r=>r.accountingOk),ledgerOk:runs.every(r=>r.ledgerOk),gdpIdentityArithmetic:runs.every(r=>r.gdpOk),normalizationActivated:runs.every(r=>r.normalizationApps>0),ok:true},compact:runs};if(outputJson){mkdirSync(dirname(outputJson),{recursive:true});writeFileSync(outputJson,JSON.stringify(report,null,2));}console.log(JSON.stringify(report,null,2));