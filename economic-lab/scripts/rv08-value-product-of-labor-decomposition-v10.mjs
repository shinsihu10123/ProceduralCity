import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { EconomicWorld } from '../src/core/world-v10.js';
import { COUNTRY_SEEDS } from '../src/config/countries.js';

const seeds=(process.env.DIAG_SEEDS||'ECON-RV02-A').split(',').map(x=>x.trim()).filter(Boolean);
const bases=(process.env.DIAG_BASES||'consumer,materials-consumer').split(',').map(x=>x.trim()).filter(Boolean);
const months=Math.max(8,Number(process.env.DIAG_MONTHS||18));
const mode=(process.env.DIAG_MODE||'canonical').trim();
const outputJson=process.env.OUTPUT_JSON?resolve(process.env.OUTPUT_JSON):null;
const EPS=1e-8,TOL=1e-7;
const F=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
const C=v=>structuredClone(v);
const S=a=>a.reduce((s,v)=>s+F(v),0);
const M=a=>a.length?S(a)/a.length:0;
const Q=(a,p)=>{if(!a.length)return 0;const x=[...a].sort((u,v)=>u-v),i=Math.max(0,Math.min(x.length-1,Math.floor((x.length-1)*p)));return x[i];};
const CL=(x,l,h)=>Math.max(l,Math.min(h,F(x)));

function transformedSeeds(){return COUNTRY_SEEDS.map(s=>({...s,initialPrice:Math.max(EPS,F(s.initialWage,F(s.initialPrice,1)))}));}
function makeWorld(seed){const old=COUNTRY_SEEDS.map(C);COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...transformedSeeds());try{return new EconomicWorld(seed,{scaleProfile:'baseline',healthCheckInterval:0});}finally{COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...old);}}
function targetSectors(base){return base==='consumer'?new Set(['CONSUMER']):new Set(['MATERIALS','CONSUMER']);}
function supplierMean(c,p){const a=c.firms.filter(f=>f.active!==false&&f.product===p&&F(f.price)>EPS);return a.length?M(a.map(f=>f.price)):0;}
function unconstrainedPlan(f){const anchor=Math.max(2,F(f.previousSales),F(f.targetInventory)*.42),expected=anchor*(1+CL(f.beliefs?.demandGrowth||0,-.18,.22)),replen=Math.max(0,F(f.targetInventory)-F(f.inventory));return Math.max(0,expected*.72+replen);}
function oneWorkerCapacity(c,f){const capitalEffect=.72+Math.log1p(Math.max(0,F(f.capitalStock)))*.105,humanEffect=.82+F(c.humanCapital)*.30,resourceEffect=f.industryId==='RESOURCE'?.62+F(c.resourceBase)*.62:1,planEffect=1+CL(f.currentPlan?.productionChange||0,-.12,.15);return Math.max(EPS,F(f.productivity)*capitalEffect*humanEffect*resourceEffect*planEffect);}
function installNormalization(w,base){const target=targetSectors(base),done=new Set();w.__abNorm=0;const original=w.supply.planProduction.bind(w.supply);w.supply.planProduction=c=>{const out=original(c);if(done.has(c.id))return out;const prices={raw_material:supplierMean(c,'raw_material'),processed_material:supplierMean(c,'processed_material')};for(const f of c.firms.filter(x=>x.active!==false&&target.has(x.industryId))){const inputCost=F(f.inputPerOutput)*(f.inputProduct?F(prices[f.inputProduct]):0),margin=F(f.price)-inputCost,payroll=F(f.wage)*F(f.workers),baseCapacity=F(f.capacity),required=margin>EPS&&baseCapacity>EPS?payroll/(margin*baseCapacity):Infinity,factor=Number.isFinite(required)?Math.max(1,required):1;if(factor>1+TOL){f.productivity*=factor;f.capacity=baseCapacity*factor;f.desiredProduction=Math.max(0,Math.min(f.capacity*1.08,unconstrainedPlan(f)));w.__abNorm++;}}done.add(c.id);return out;};}
function installPrior(w){w.__abPrior=new Map();const begin=w.supply.beginMonth.bind(w.supply);w.supply.beginMonth=c=>{for(const f of c.firms)w.__abPrior.set(`${c.id}|${f.id}`,{workers:F(f.workers),sales:F(f.sales),revenue:F(f.revenue),inputSpend:F(f.inputSpend)});return begin(c);};}
function resize(c,f,target){target=Math.max(0,Math.min(Math.floor(target),Math.floor(F(f.workers))));const employees=c.households.filter(h=>h.employed&&h.employerId===f.id).sort((a,b)=>a.id.localeCompare(b.id)),keep=new Set(employees.slice(0,target).map(h=>h.id));for(const h of employees)if(!keep.has(h.id)){h.employed=false;h.employerId=null;}f.workers=keep.size;f.desiredWorkers=f.workers;}
function deactivate(c,f){for(const h of c.households)if(h.employed&&h.employerId===f.id){h.employed=false;h.employerId=null;}f.active=false;f.desiredWorkers=0;f.desiredProduction=0;f.workers=0;}
function restructureSignal(w,c,f){const cash=w.ledger.balance(f.accountId),workers=Math.max(0,F(f.workers)),wage=Math.max(EPS,F(f.wage)),severePayrollStress=F(f.wageArrears)>Math.max(100,wage*Math.max(1,workers)*1.35),severeCreditStress=F(f.creditMisses)>=5,liquidityFailure=cash<F(f.safeCash)*.025&&severePayrollStress,nextDistress=(liquidityFailure||severeCreditStress)?F(f.distressMonths)+1:Math.max(0,F(f.distressMonths)-1),inputPrice=f.inputProduct?supplierMean(c,f.inputProduct):0,margin=F(f.price)-F(f.inputPerOutput)*inputPrice,pos=Math.max(0,margin),realized=Math.max(0,F(f.revenue)-F(f.inputSpend)),prior=w.__abPrior.get(`${c.id}|${f.id}`)||{},recent=pos*Math.max(0,F(prior.sales)),capacity=pos*Math.max(0,F(f.capacity)),support=Math.max(realized,recent,capacity),target=workers>0?Math.max(0,Math.min(workers,Math.floor((support+TOL)/wage))):0;return{severeCreditStress,nextDistress,target};}
function installExitMode(w){w.__abExits=0;w.__abRestructures=0;if(mode!=='restructure'){const orig=w.supply.evaluateExits.bind(w.supply);w.supply.evaluateExits=c=>{const x=orig(c);w.__abExits+=x.length;return x;};return;}w.supply.evaluateExits=c=>{const exited=[];for(const f of c.firms.filter(x=>x.active!==false)){const s=restructureSignal(w,c,f);f.distressMonths=s.nextDistress;if(f.distressMonths<4)continue;if(!s.severeCreditStress&&s.target>0){resize(c,f,s.target);f.distressMonths=0;w.__abRestructures++;}else{deactivate(c,f);exited.push(f.industryId);w.__abExits++;}}return exited;};}
function installAudit(w,base){w.__abRows=[];const orig=w.supply.procureInputs.bind(w.supply);w.supply.procureInputs=(c,month)=>{for(const f of c.firms.filter(x=>x.active!==false)){const plan=unconstrainedPlan(f);if(plan<=EPS)continue;const wage=Math.max(EPS,F(f.wage)),price=Math.max(EPS,F(f.price)),inputPrice=f.inputProduct?supplierMean(c,f.inputProduct):0,inputObserved=!f.inputProduct||inputPrice>EPS,inputCostPerUnit=F(f.inputPerOutput)*inputPrice,unitMargin=price-inputCostPerUnit,cap1=oneWorkerCapacity(c,f),physical=Math.max(1,Math.ceil(plan/cap1)),grossValueProduct=price*cap1/wage,netValueProduct=Math.max(0,unitMargin)*cap1/wage,expectedContribution=Math.max(0,unitMargin)*plan,physicalPayroll=physical*wage,planPayrollCoverage=expectedContribution/Math.max(EPS,physicalPayroll),planUtilization=plan/Math.max(EPS,physical*cap1);let classification='plan_viable';if(!inputObserved)classification='missing_input_price';else if(unitMargin<=EPS)classification='nonpositive_unit_margin';else if(netValueProduct<1-TOL)classification='value_product_below_wage';else if(planPayrollCoverage<1-TOL)classification='worker_indivisibility_or_low_utilization';w.__abRows.push({month:w.month,countryId:c.id,firmId:f.id,industryId:f.industryId,base,plan,wage,price,inputPrice,inputCostPerUnit,unitMargin,oneWorkerCapacity:cap1,physical,grossValueProduct,netValueProduct,expectedContribution,physicalPayroll,planPayrollCoverage,planUtilization,wageToPrice:wage/price,classification});}return orig(c,month);};}
function gdpResidual(m){return F(m?.gdp)-(F(m?.consumption)+F(m?.grossInvestment)+F(m?.publicInvestment)+F(m?.governmentConsumption)+F(m?.inventoryInvestment)+F(m?.netExports));}
function summarize(rows){const a=rows.filter(r=>r.physical>0),classes={};for(const r of a)classes[r.classification]=(classes[r.classification]||0)+1;for(const k of Object.keys(classes))classes[k]/=Math.max(1,a.length);return{rows:a.length,meanPhysical:M(a.map(r=>r.physical)),meanPlan:M(a.map(r=>r.plan)),meanOneWorkerCapacity:M(a.map(r=>r.oneWorkerCapacity)),meanGrossValueProduct:M(a.map(r=>r.grossValueProduct)),meanNetValueProduct:M(a.map(r=>r.netValueProduct)),p50NetValueProduct:Q(a.map(r=>r.netValueProduct),.5),p25NetValueProduct:Q(a.map(r=>r.netValueProduct),.25),meanPlanPayrollCoverage:M(a.map(r=>r.planPayrollCoverage)),p50PlanPayrollCoverage:Q(a.map(r=>r.planPayrollCoverage),.5),meanPlanUtilization:M(a.map(r=>r.planUtilization)),meanWageToPrice:M(a.map(r=>r.wageToPrice)),shareNetValueProductBelow1:a.length?a.filter(r=>r.netValueProduct<1-TOL).length/a.length:0,shareLowUtilizationGap:a.length?a.filter(r=>r.classification==='worker_indivisibility_or_low_utilization').length/a.length:0,sharePlanViable:a.length?a.filter(r=>r.classification==='plan_viable').length/a.length:0,classShares:classes};}
function run(base,seed){const w=makeWorld(seed);for(const c of w.countries)Object.defineProperty(c,'__diagnosticExactLaborRuntime',{value:true,writable:true,configurable:true,enumerable:false});installNormalization(w,base);installPrior(w);installAudit(w,base);installExitMode(w);const monthly=[];for(let i=0;i<months;i++){w.stepMonth();monthly.push({month:w.month,unemployment:M(w.countries.map(c=>F(c.macro?.unemployment))),arrears:M(w.countries.map(c=>F(c.macro?.wageArrears))),gdp:M(w.countries.map(c=>F(c.macro?.gdp))),output:S(w.countries.flatMap(c=>c.firms.filter(f=>f.active!==false).map(f=>F(f.output))))});}const health=w.forceHealthCheck();assert.ok(health.ok,`${base}/${seed}: health`);const accountingOk=w.countries.every(c=>w.accounting.verifyCountry(c,w.ledger,w.month)?.ok!==false),ledgerOk=w.countries.every(c=>w.ledger.verifyCountry(c.id)?.ok===true),gdpOk=w.countries.every(c=>Math.abs(gdpResidual(c.macro))<1e-5);const industry=Object.fromEntries([...new Set(w.__abRows.map(r=>r.industryId))].sort().map(ind=>[ind,summarize(w.__abRows.filter(r=>r.industryId===ind))]));return{base,seed,mode,months,health,accountingOk,ledgerOk,gdpOk,normalizationApps:w.__abNorm||0,exits:w.__abExits||0,restructures:w.__abRestructures||0,monthly,summary:summarize(w.__abRows),industry};}

const runs=[];for(const base of bases)for(const seed of seeds)runs.push(run(base,seed));
const gates={allHealthy:runs.every(r=>r.health.ok),completeCoverage:runs.length===bases.length*seeds.length,normalizationActivated:runs.every(r=>r.normalizationApps>0),ledgerCountriesOk:runs.every(r=>r.ledgerOk),generalAccountingOk:runs.every(r=>r.accountingOk),gdpIdentityArithmetic:runs.every(r=>r.gdpOk),auditObserved:runs.every(r=>r.summary.rows>0),finiteRows:runs.every(r=>Object.values(r.summary).every(v=>typeof v!=='number'||Number.isFinite(v)))};gates.ok=Object.values(gates).every(Boolean);assert.ok(gates.ok,`R4-AB gates ${JSON.stringify(gates)}`);
const compact=runs.map(r=>({base:r.base,seed:r.seed,mode:r.mode,u:M(r.monthly.map(x=>x.unemployment)),arrears:M(r.monthly.map(x=>x.arrears)),gdp:M(r.monthly.map(x=>x.gdp)),output:M(r.monthly.map(x=>x.output)),exits:r.exits,restructures:r.restructures,...r.summary}));
console.log('WP-RV08-R4-AB gates',gates);console.table(compact.map(x=>({base:x.base,seed:x.seed,u:x.u,vpl:x.meanNetValueProduct,vplP50:x.p50NetValueProduct,planCover:x.meanPlanPayrollCoverage,util:x.meanPlanUtilization,below1:x.shareNetValueProductBelow1,lowUtil:x.shareLowUtilizationGap,viable:x.sharePlanViable})));console.log('WP-RV08-R4-AB: PASS');
const payload={workPackage:'WP-RV08-R4-AB',title:'Value Product of Labor Decomposition',note:'Read-only diagnostic. Net value product is current unit contribution margin times one-worker physical capacity divided by wage. It is an internal decomposition, not an empirical productivity calibration.',generatedAt:new Date().toISOString(),configuration:{mode,bases,seeds,months},gates,compact,runs:runs.map(r=>({base:r.base,seed:r.seed,mode:r.mode,months:r.months,exits:r.exits,restructures:r.restructures,summary:r.summary,industry:r.industry}))};if(outputJson){mkdirSync(dirname(outputJson),{recursive:true});writeFileSync(outputJson,JSON.stringify(payload,null,2));console.log('WP_RV08_R4_AB_OUTPUT',outputJson);}