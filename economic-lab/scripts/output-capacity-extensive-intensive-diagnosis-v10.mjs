import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { EconomicWorld } from '../src/core/world-v10.js';
import { COUNTRY_SEEDS } from '../src/config/countries.js';

const scales=(process.env.DIAG_SCALES||'compact,baseline').split(',').map(x=>x.trim()).filter(Boolean);
const seeds=(process.env.DIAG_SEEDS||'ECON-RV02-A,ECON-RV02-B,ECON-RV02-C').split(',').map(x=>x.trim()).filter(Boolean);
const months=Math.max(1,Number(process.env.DIAG_MONTHS||12));
const outputJson=process.env.OUTPUT_JSON?resolve(process.env.OUTPUT_JSON):null;
const EPS=1e-8,TOL=1e-7;
const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
const sum=a=>a.reduce((s,v)=>s+finite(v),0),mean=a=>a.length?sum(a)/a.length:0,ratio=(a,b)=>Math.abs(finite(b))>EPS?finite(a)/finite(b):0,clone=v=>structuredClone(v),clamp=(x,lo,hi)=>Math.max(lo,Math.min(hi,finite(x)));
function transformedSeeds(){return COUNTRY_SEEDS.map(seed=>({...seed,initialPrice:Math.max(EPS,finite(seed.initialWage,finite(seed.initialPrice,1)))}));}
function createWorld(scaleProfile,seedText){const original=COUNTRY_SEEDS.map(clone);COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...transformedSeeds());try{return new EconomicWorld(seedText,{scaleProfile,healthCheckInterval:0});}finally{COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...original);}}
function gdpResidual(m){return finite(m?.gdp)-(finite(m?.consumption)+finite(m?.grossInvestment)+finite(m?.publicInvestment)+finite(m?.governmentConsumption)+finite(m?.inventoryInvestment)+finite(m?.netExports));}
function fingerprint(w){return {month:w.month,rng:clone(w.rng),countries:clone(w.countries),ledgerEntries:clone(w.ledger.entries),accounting:w.countries.map(c=>({id:c.id,report:w.accountingReport(c.id)}))};}
function unconstrainedPlan(f){const demandAnchor=Math.max(2,finite(f.previousSales),finite(f.targetInventory)*0.42),expected=demandAnchor*(1+clamp(f.beliefs?.demandGrowth||0,-0.18,0.22)),replenishment=Math.max(0,finite(f.targetInventory)-finite(f.inventory));return Math.max(0,expected*0.72+replenishment);}
function installAudit(world){
  world.__rv07P22={plan:new Map(),produce:new Map()};
  const plan=world.supply.planProduction.bind(world.supply);
  world.supply.planProduction=country=>{const out=plan(country);const rows=country.firms.filter(f=>f.active!==false).map(f=>{const u=unconstrainedPlan(f),desired=finite(f.desiredProduction),cap=finite(f.capacity);return {firmId:f.id,industryId:f.industryId,workers:finite(f.workers),capacity:cap,capacityCap:cap*1.08,unconstrainedPlan:u,desiredProduction:desired,desiredError:desired-Math.min(cap*1.08,u),capacityBound:u>cap*1.08+TOL,inputProduct:f.inputProduct||null};});world.__rv07P22.plan.set(`${world.month}|${country.id}`,rows);return out;};
  const produce=world.supply.produce.bind(world.supply);
  world.supply.produce=(country,month,metrics)=>{const out=produce(country,month,metrics);world.__rv07P22.produce.set(`${month}|${country.id}`,new Map(country.firms.map(f=>[f.id,{output:finite(f.output),supplyShortage:finite(f.supplyShortage)}])));return out;};
}
function aggregateRows(rows){return {firms:rows.length,workers:sum(rows.map(r=>r.workers)),workersPerFirm:ratio(sum(rows.map(r=>r.workers)),rows.length),capacity:sum(rows.map(r=>r.capacity)),capacityPerFirm:ratio(sum(rows.map(r=>r.capacity)),rows.length),unconstrainedPlan:sum(rows.map(r=>r.unconstrainedPlan)),desiredProduction:sum(rows.map(r=>r.desiredProduction)),output:sum(rows.map(r=>r.output)),capacitySuppression:sum(rows.map(r=>Math.max(0,r.unconstrainedPlan-r.desiredProduction))),inputOrExecutionSuppression:sum(rows.map(r=>Math.max(0,r.desiredProduction-r.output))),capacityBoundShare:ratio(rows.filter(r=>r.capacityBound).length,rows.length),desiredToPlan:ratio(sum(rows.map(r=>r.desiredProduction)),sum(rows.map(r=>r.unconstrainedPlan)),outputToDesired:ratio(sum(rows.map(r=>r.output)),sum(rows.map(r=>r.desiredProduction)),outputPerFirm:ratio(sum(rows.map(r=>r.output)),rows.length),outputPerWorker:ratio(sum(rows.map(r=>r.output)),sum(rows.map(r=>r.workers)),inputConstrainedShare:ratio(rows.filter(r=>r.inputProduct&&r.output+TOL<r.desiredProduction&&r.supplyShortage>TOL).length,rows.length)};}
function runWorld(scaleProfile,seed,horizon,audited){
  const world=createWorld(scaleProfile,seed);if(audited)installAudit(world);const firmRows=[],countryRows=[];
  for(let i=0;i<horizon;i++){world.stepMonth();for(const c of world.countries){if(audited){const key=`${world.month}|${c.id}`,plans=world.__rv07P22.plan.get(key)||[],prod=world.__rv07P22.produce.get(key)||new Map();assert.ok(plans.length>0,`${key}: plan rows missing`);for(const p of plans){const q=prod.get(p.firmId);assert.ok(q,`${key}/${p.firmId}: production row missing`);const f=c.firms.find(x=>x.id===p.firmId);firmRows.push({scaleProfile,seed,month:world.month,countryId:c.id,...p,...q,activeEnd:f?.active!==false,exitedByEnd:f?.active===false});}const joined=firmRows.filter(r=>r.scaleProfile===scaleProfile&&r.seed===seed&&r.month===world.month&&r.countryId===c.id),agg=aggregateRows(joined);const sectorOutput=sum(Object.values(c.lastIndustry?.sectorOutputs||{}));countryRows.push({scaleProfile,seed,month:world.month,countryId:c.id,...agg,activeEnd:c.firms.filter(f=>f.active!==false).length,exitThisMonth:finite(c.macro?.firmExits),sectorOutput,sectorOutputError:agg.output-sectorOutput,unemployment:finite(c.macro?.unemployment),wageArrears:finite(c.macro?.wageArrears),gdpResidual:gdpResidual(c.macro),ledgerOk:world.ledger.verifyCountry(c.id)?.ok===true});}}
    }
  const health=world.forceHealthCheck();assert.ok(health.ok,`${scaleProfile}/${seed}: health failed`);return {world,firmRows,countryRows,health,fingerprint:fingerprint(world)};
}
const ni=[];for(const s of scales){const seed=`ECON-RV07-P22-NI-${s}`,h=Math.min(3,months);const a=runWorld(s,seed,h,false).fingerprint,b=runWorld(s,seed,h,true).fingerprint,exact=JSON.stringify(a)===JSON.stringify(b);assert.ok(exact,`${s}: observer interference`);ni.push({scaleProfile:s,exact});}
const runs=[];for(const s of scales)for(const seed of seeds)runs.push(runWorld(s,seed,months,true));const firmRows=runs.flatMap(r=>r.firmRows),countryRows=runs.flatMap(r=>r.countryRows);
const windows=[{id:'M1-3',from:1,to:Math.min(3,months)},{id:'M4-6',from:4,to:Math.min(6,months)},{id:'M7-9',from:7,to:Math.min(9,months)},{id:'M10-12',from:10,to:months},{id:'FULL',from:1,to:months}].filter(w=>w.from<=w.to);
function aggWindow(rs){return {countryMonths:rs.length,meanFirms:mean(rs.map(r=>r.firms)),meanActiveEnd:mean(rs.map(r=>r.activeEnd)),totalExits:sum(rs.map(r=>r.exitThisMonth)),meanWorkers:mean(rs.map(r=>r.workers)),meanWorkersPerFirm:mean(rs.map(r=>r.workersPerFirm)),meanCapacity:mean(rs.map(r=>r.capacity)),meanCapacityPerFirm:mean(rs.map(r=>r.capacityPerFirm)),meanUnconstrainedPlan:mean(rs.map(r=>r.unconstrainedPlan)),meanDesiredProduction:mean(rs.map(r=>r.desiredProduction)),meanOutput:mean(rs.map(r=>r.output)),meanOutputPerFirm:mean(rs.map(r=>r.outputPerFirm)),meanOutputPerWorker:mean(rs.map(r=>r.outputPerWorker)),meanCapacitySuppression:mean(rs.map(r=>r.capacitySuppression)),meanInputOrExecutionSuppression:mean(rs.map(r=>r.inputOrExecutionSuppression)),meanCapacityBoundShare:mean(rs.map(r=>r.capacityBoundShare)),meanDesiredToPlan:mean(rs.map(r=>r.desiredToPlan)),meanOutputToDesired:mean(rs.map(r=>r.outputToDesired)),meanInputConstrainedShare:mean(rs.map(r=>r.inputConstrainedShare)),meanUnemployment:mean(rs.map(r=>r.unemployment))};}
const summary=[];for(const s of scales)for(const w of windows)summary.push({scaleProfile:s,window:w.id,...aggWindow(countryRows.filter(r=>r.scaleProfile===s&&r.month>=w.from&&r.month<=w.to))});
const industrySummary=[];for(const s of scales)for(const w of windows)for(const id of ['RESOURCE','MATERIALS','CAPITAL','CONSUMER']){const groups=[];for(const seed of seeds)for(let m=w.from;m<=w.to;m++)for(const countryId of ['AST','BRN','CYR','DRN']){const rs=firmRows.filter(r=>r.scaleProfile===s&&r.seed===seed&&r.month===m&&r.countryId===countryId&&r.industryId===id);if(rs.length)groups.push(aggregateRows(rs));}industrySummary.push({scaleProfile:s,window:w.id,industryId:id,...aggWindow(groups.map((g,i)=>({...g,activeEnd:g.firms,exitThisMonth:0,unemployment:0})))});}
const maxDesiredError=Math.max(0,...firmRows.map(r=>Math.abs(r.desiredError))),maxSectorOutputError=Math.max(0,...countryRows.map(r=>Math.abs(r.sectorOutputError))),maxGdpResidual=Math.max(0,...countryRows.map(r=>Math.abs(r.gdpResidual)));
const gates={observerNonInterferenceExact:ni.every(x=>x.exact),allHealthy:runs.every(r=>r.health?.ok===true),completeCountryCoverage:countryRows.length===scales.length*seeds.length*months*4,firmRowsPresent:firmRows.length>0,desiredProductionReconciled:maxDesiredError<TOL,sectorOutputReconciled:maxSectorOutputError<TOL,ledgerCountriesOk:countryRows.every(r=>r.ledgerOk),gdpIdentityReconciled:maxGdpResidual<TOL,finiteRows:countryRows.every(r=>Number.isFinite(r.output)&&Number.isFinite(r.capacity))};gates.ok=Object.values(gates).every(Boolean);assert.ok(gates.ok,`WP-RV07-P22 gates failed: ${JSON.stringify(gates)}`);
console.table(summary.map(x=>({scale:x.scaleProfile,window:x.window,firms:+x.meanFirms.toFixed(1),activeEnd:+x.meanActiveEnd.toFixed(1),exits:x.totalExits,workers:+x.meanWorkers.toFixed(1),wPerFirm:+x.meanWorkersPerFirm.toFixed(2),capacity:+x.meanCapacity.toFixed(1),capPerFirm:+x.meanCapacityPerFirm.toFixed(2),plan:+x.meanUnconstrainedPlan.toFixed(1),desired:+x.meanDesiredProduction.toFixed(1),output:+x.meanOutput.toFixed(1),outPerFirm:+x.meanOutputPerFirm.toFixed(2),capSuppress:+x.meanCapacitySuppression.toFixed(1),inputSuppress:+x.meanInputOrExecutionSuppression.toFixed(1),capBound:+x.meanCapacityBoundShare.toFixed(3),outDesired:+x.meanOutputToDesired.toFixed(3)})));
console.log('WP_RV07_P22_GATES',JSON.stringify(gates));
const payload={workPackage:'WP-RV07-P22',title:'Output collapse extensive/intensive capacity decomposition',generatedAt:new Date().toISOString(),configuration:{scales,seeds,months},nonInterference:ni,gates,reconciliation:{maxDesiredError,maxSectorOutputError,maxGdpResidual},summary,industrySummary,countryRows,firmRows};if(outputJson){mkdirSync(dirname(outputJson),{recursive:true});writeFileSync(outputJson,JSON.stringify(payload,null,2));console.log('WP_RV07_P22_OUTPUT',outputJson);}
