import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { EconomicWorld } from '../src/core/world-v10.js';
import { COUNTRY_SEEDS } from '../src/config/countries.js';

const scales=(process.env.DIAG_SCALES||'compact,baseline').split(',').map(x=>x.trim()).filter(Boolean);
const seeds=(process.env.DIAG_SEEDS||'ECON-RV02-A,ECON-RV02-B,ECON-RV02-C').split(',').map(x=>x.trim()).filter(Boolean);
const months=Math.max(1,Number(process.env.DIAG_MONTHS||12)),outputJson=process.env.OUTPUT_JSON?resolve(process.env.OUTPUT_JSON):null;
const EPS=1e-8,TOL=1e-7,finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f,clone=v=>structuredClone(v),sum=a=>a.reduce((s,v)=>s+finite(v),0),mean=a=>a.length?sum(a)/a.length:0,ratio=(a,b)=>Math.abs(finite(b))>EPS?finite(a)/finite(b):0;
const targets={
 'unit-basis-control':[],
 'unit-basis-materials-input-free':['MATERIALS'],
 'unit-basis-consumer-input-free':['CONSUMER'],
 'unit-basis-downstream-input-free':['MATERIALS','CONSUMER'],
 'unit-basis-capital-input-free':['CAPITAL'],
 'unit-basis-all-input-free':['MATERIALS','CONSUMER','CAPITAL']
};
const variants=Object.keys(targets);
function transformedSeeds(){return COUNTRY_SEEDS.map(s=>({...s,initialPrice:Math.max(EPS,finite(s.initialWage,finite(s.initialPrice,1)))}));}
function createWorld(scale,seed){const original=COUNTRY_SEEDS.map(clone);COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...transformedSeeds());try{return new EconomicWorld(seed,{scaleProfile:scale,healthCheckInterval:0});}finally{COUNTRY_SEEDS.splice(0,COUNTRY_SEEDS.length,...original);}}
function install(w,v){const t=new Set(targets[v]);w.__rv07P38={applications:0,checks:[]};for(const c of w.countries)for(const f of c.firms.filter(x=>x.active!==false)){const before={inputProduct:f.inputProduct,inputPerOutput:finite(f.inputPerOutput)};if(t.has(f.industryId)){f.inputProduct=null;f.inputPerOutput=0;f.inputInventory={};f.inputBookValues={};w.__rv07P38.applications++;}w.__rv07P38.checks.push({countryId:c.id,firmId:f.id,industryId:f.industryId,targeted:t.has(f.industryId),beforeInputProduct:before.inputProduct,beforeInputPerOutput:before.inputPerOutput,afterInputProduct:f.inputProduct,afterInputPerOutput:finite(f.inputPerOutput)});}}
function gdpResidual(m){return finite(m?.gdp)-(finite(m?.consumption)+finite(m?.grossInvestment)+finite(m?.publicInvestment)+finite(m?.governmentConsumption)+finite(m?.inventoryInvestment)+finite(m?.netExports));}
function fingerprint(w){return JSON.stringify({month:w.month,rng:w.rng,countries:w.countries,ledger:w.ledger.entries,acct:w.countries.map(c=>w.accountingReport(c.id))});}
function run(v,scale,seed,h,capture=false){const w=createWorld(scale,seed);install(w,v);const rows=[];for(let i=0;i<h;i++){w.stepMonth();for(const c of w.countries)rows.push({variant:v,scaleProfile:scale,seed,month:w.month,countryId:c.id,unemployment:finite(c.macro?.unemployment),exits:finite(c.macro?.firmExits),wageArrears:finite(c.macro?.wageArrears),goodsFulfillment:1-finite(c.macro?.unmetDemandRatio),inputShortage:finite(c.macro?.inputShortageUnits),b2bTrade:finite(c.macro?.b2bTrade),resourceOutput:finite(c.macro?.resourceOutput),materialsOutput:finite(c.macro?.materialsOutput),capitalOutput:finite(c.macro?.capitalGoodsOutput),consumerOutput:finite(c.macro?.consumerGoodsOutput),firmCash:finite(c.macro?.firmCash),nominalSales:finite(c.macro?.nominalSales),gdp:finite(c.macro?.gdp),gdpResidual:gdpResidual(c.macro),ledgerOk:w.ledger.verifyCountry(c.id)?.ok===true});}const health=w.forceHealthCheck();assert.ok(health.ok,`${v}/${scale}/${seed} health`);return{variant:v,scaleProfile:scale,seed,rows,health,applications:w.__rv07P38.applications,checks:w.__rv07P38.checks,fingerprint:capture?fingerprint(w):null};}
const determinism=[];for(const v of variants)for(const scale of scales){const seed=`ECON-RV07-P38-DET-${v}-${scale}`,h=Math.min(3,months),a=run(v,scale,seed,h,true).fingerprint,b=run(v,scale,seed,h,true).fingerprint;const exact=a===b;assert.ok(exact,`${v}/${scale} nondeterministic`);determinism.push({variant:v,scaleProfile:scale,exact});}
const runs=[];for(const v of variants)for(const scale of scales)for(const seed of seeds)runs.push(run(v,scale,seed,months,false));const rows=runs.flatMap(r=>r.rows);
const windows=[['M1-3',1,Math.min(3,months)],['M4-6',4,Math.min(6,months)],['M7-9',7,Math.min(9,months)],['M10-12',10,months],['FULL',1,months]].filter(x=>x[1]<=x[2]);
function agg(rs){return{meanUnemployment:mean(rs.map(r=>r.unemployment)),totalExits:sum(rs.map(r=>r.exits)),meanWageArrears:mean(rs.map(r=>r.wageArrears)),meanGoodsFulfillment:mean(rs.map(r=>r.goodsFulfillment)),meanInputShortage:mean(rs.map(r=>r.inputShortage)),meanB2bTrade:mean(rs.map(r=>r.b2bTrade)),meanResourceOutput:mean(rs.map(r=>r.resourceOutput)),meanMaterialsOutput:mean(rs.map(r=>r.materialsOutput)),meanCapitalOutput:mean(rs.map(r=>r.capitalOutput)),meanConsumerOutput:mean(rs.map(r=>r.consumerOutput)),meanFirmCash:mean(rs.map(r=>r.firmCash)),meanNominalSales:mean(rs.map(r=>r.nominalSales)),meanGdp:mean(rs.map(r=>r.gdp))};}
const summary=[];for(const v of variants)for(const scale of scales)for(const [window,from,to] of windows)summary.push({variant:v,scaleProfile:scale,window,...agg(rows.filter(r=>r.variant===v&&r.scaleProfile===scale&&r.month>=from&&r.month<=to))});
const comparison={};for(const scale of scales){comparison[scale]={};for(const v of variants.filter(x=>x!=='unit-basis-control')){comparison[scale][v]={};for(const [window] of windows){const c=summary.find(x=>x.variant==='unit-basis-control'&&x.scaleProfile===scale&&x.window===window),a=summary.find(x=>x.variant===v&&x.scaleProfile===scale&&x.window===window);comparison[scale][v][window]={unemploymentDifference:a.meanUnemployment-c.meanUnemployment,exitDifference:a.totalExits-c.totalExits,wageArrearsDifference:a.meanWageArrears-c.meanWageArrears,goodsFulfillmentDifference:a.meanGoodsFulfillment-c.meanGoodsFulfillment,inputShortageDifference:a.meanInputShortage-c.meanInputShortage,b2bTradeDifference:a.meanB2bTrade-c.meanB2bTrade,resourceOutputRatio:ratio(a.meanResourceOutput,c.meanResourceOutput),materialsOutputRatio:ratio(a.meanMaterialsOutput,c.meanMaterialsOutput),consumerOutputRatio:ratio(a.meanConsumerOutput,c.meanConsumerOutput),firmCashDifference:a.meanFirmCash-c.meanFirmCash,nominalSalesRatio:ratio(a.meanNominalSales,c.meanNominalSales),gdpDifference:a.meanGdp-c.meanGdp};}}}
const checks=runs.flatMap(r=>r.checks.map(x=>({...x,variant:r.variant}))),nonTargetChanged=checks.some(x=>!x.targeted&&(x.beforeInputProduct!==x.afterInputProduct||Math.abs(x.beforeInputPerOutput-x.afterInputPerOutput)>TOL)),targetNotFree=checks.some(x=>x.targeted&&(x.afterInputProduct!==null||Math.abs(x.afterInputPerOutput)>TOL)),maxGdpResidual=Math.max(0,...rows.map(r=>Math.abs(r.gdpResidual)));
const gates={deterministicReplayExact:determinism.every(x=>x.exact),allHealthy:runs.every(r=>r.health?.ok===true),completeCoverage:rows.length===variants.length*scales.length*seeds.length*months*4,everyNonControlVariantActivated:variants.filter(v=>v!=='unit-basis-control').every(v=>runs.filter(r=>r.variant===v).reduce((s,r)=>s+r.applications,0)>0),nonTargetInputStructuresUnchanged:!nonTargetChanged,targetInputsFullyRemoved:!targetNotFree,ledgerCountriesOk:rows.every(r=>r.ledgerOk),gdpIdentityReconciled:maxGdpResidual<TOL,finiteRows:rows.every(r=>Number.isFinite(r.unemployment)&&Number.isFinite(r.consumerOutput))};gates.ok=Object.values(gates).every(Boolean);assert.ok(gates.ok,`WP-RV07-P38 gates failed ${JSON.stringify(gates)}`);
console.table(summary.filter(x=>x.window==='FULL').map(x=>({variant:x.variant,scale:x.scaleProfile,u:+x.meanUnemployment.toFixed(4),exits:x.totalExits,arrears:+x.meanWageArrears.toFixed(0),fulfill:+x.meanGoodsFulfillment.toFixed(3),short:+x.meanInputShortage.toFixed(1),materials:+x.meanMaterialsOutput.toFixed(1),consumer:+x.meanConsumerOutput.toFixed(1),cash:+x.meanFirmCash.toFixed(0)})));
console.log('WP_RV07_P38_COMPARISON',JSON.stringify(comparison));console.log('WP_RV07_P38_GATES',JSON.stringify(gates));
const payload={workPackage:'WP-RV07-P38',title:'Intermediate-input-free causal upper-bound matrix',generatedAt:new Date().toISOString(),configuration:{variants,targets,scales,seeds,months},determinism,gates,reconciliation:{maxGdpResidual},summary,comparison,interventions:runs.map(r=>({variant:r.variant,scaleProfile:r.scaleProfile,seed:r.seed,applications:r.applications})),rows};if(outputJson){mkdirSync(dirname(outputJson),{recursive:true});writeFileSync(outputJson,JSON.stringify(payload,null,2));console.log('WP_RV07_P38_OUTPUT',outputJson);}
