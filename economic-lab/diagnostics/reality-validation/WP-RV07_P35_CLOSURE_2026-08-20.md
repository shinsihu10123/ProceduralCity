# WP-RV07-P35 Closure — 2026-08-20

## Verdict

**PASS — BREAK-EVEN PHYSICAL CAPACITY IS CAUSALLY MATERIAL BUT NOT SUFFICIENT ALONE**

Run: `32319610598`  
Job: `96278985711`  
Artifact: `economic-lab-wp-rv07-p35`, ID `9389349540`, SHA-256 `d2a622bf868597da2bfe5a5241db9df0c163be3861944f46cea3cb9878b752c8`

All hard gates passed: deterministic replay, health, coverage, intervention activation, non-target isolation, break-even capacity floor, desired-production replan exactness, ledger health, GDP identity, finite rows.

## A — VERIFIED EXISTING FACTS / RESULTS

1. Unit-basis control, baseline, 12-month pooled result: unemployment `0.2514`, exits `248`, wage arrears `64933.2`, goods fulfillment `0.5575`, mean consumer output `118.6`, mean firm cash `137746.9`.
2. RESOURCE-only break-even capacity normalization raises RESOURCE output to `1.308x` control but leaves unemployment essentially unchanged (`+0.00107`) and adds 7 exits.
3. MATERIALS-only normalization raises MATERIALS output to `1.334x`; unemployment is slightly worse (`+0.00714`) and exits +5.
4. CONSUMER-only normalization is materially beneficial: unemployment `-0.03205`, exits `-21`, wage arrears `-7030.4`, goods fulfillment `+0.13985`, consumer output `1.369x`, firm cash `+13329.8`.
5. Upstream joint RESOURCE+MATERIALS normalization raises outputs strongly but does not reduce pooled unemployment (`+0.00922`); it improves late goods fulfillment and cash.
6. Non-capital joint RESOURCE+MATERIALS+CONSUMER normalization is the strongest P35 variant: baseline unemployment `0.2350` vs `0.2514`, exits `224` vs `248`, wage arrears `56608.2` vs `64933.2`, goods fulfillment `0.7756` vs `0.5575`, RESOURCE `1.553x`, MATERIALS `1.701x`, CONSUMER `1.658x`, firm cash `+23389.6`.
7. Even the strongest non-capital capacity normalization does **not** eliminate residual unemployment/exit dynamics over the 12-month horizon.

## B — DIAGNOSTIC LEADS

- Physical unit capacity is a genuine causal bottleneck, especially in CONSUMER and in the joint non-capital system.
- Sector normalization effects are highly interactive: raising upstream capacity alone can create additional downstream input pressure rather than automatically curing macro collapse.
- The residual after non-capital normalization points away from a single scalar productivity defect and toward a coupled unit-system / relative-price / input-topology / demand-feedback problem.

## C — HYPOTHESES

- H-P35-1: `physical output per worker` is one component of the structural unit-basis mismatch. **SUPPORTED**.
- H-P35-2: physical capacity insufficiency alone is the sole root cause. **FALSIFIED**.
- H-P35-3: CONSUMER production capacity is a major propagation bottleneck once initial finished-goods inventory is exhausted. **STRONGLY SUPPORTED** when read jointly with P34.
- H-P35-4: upstream capacity increases require coherent relative prices and/or intermediate-input topology to translate into stable downstream recovery. **SUPPORTED AS NEXT LEAD**.

## D — NEXT DIAGNOSTIC CHANGES

No canonical repair is authorized by this closure.

Expanded execution should now run in parallel:

1. exact unit-factor attribution (price/wage/input/capacity decomposition),
2. one-time derived break-even relative-price initialization,
3. intermediate-input-free causal upper bounds,
4. price × capacity interaction matrix.

## Controls

- Canonical economic mechanism changes: **0**
- Parameter fitting/tuning: **0**
- Repair merge: **0**
- Empirical realism claim: **NO**
