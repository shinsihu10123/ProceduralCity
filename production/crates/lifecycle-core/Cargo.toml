[package]
name = "gaonn-lifecycle-core"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
gaonn-identity-core = { path = "../identity-core" }
gaonn-world-core = { path = "../world-core" }
